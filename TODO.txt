FEATURES TO IMPLEMENT

Add better support to small devices so that two columns are shown without extra white space
make the app compatible for old api
Add material design elements from 3rd party library (¿where?)
replace R/T/C buttons with figures that are painted according to the chosen color & alpha


NEXT RELEASE:

Fix bug in quickreturnview lib that prevents the toptargetview from showing up when scrolling up the last item in the list
Add tablet support
force files to be stored to SD card if possible (instead of emulated external storage)
consider moving the app to external storage
save bitmaps (compressed and full) in async task
replace deprecated calls for vibration
prettify gallery, "chrome tabs overview"-like


